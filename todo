- Translate the help and the commands

- Support for custom maps

- Prolly support for adding workshop maps directly from Discord
    - .json "db" for urls and id's
    - .json is used as a base for all the supported maps by the bot
    - .json object itself should include
        - map-name
        - workshop-url if need be
        - the workshop id
        - what gamemode it supports
            - wingman
            - comp
            - others? [not prolly ever]
    - !scrim map add [URL]
    - !scrim map list
    - !scrim map remove [URL]

- Use some state management library for the teams 